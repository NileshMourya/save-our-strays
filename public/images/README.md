# Image assets

Every image under this directory is an auto-generated warm-toned SVG
placeholder standing in for the organisation's real photography during
the WordPress → Next.js migration. Replace each file with the matching
photo from the existing site (same filename, ideally exported as
optimized .jpg/.webp) — every component already points at these paths,
so dropping in real files with the same names is enough.
